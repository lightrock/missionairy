# Needs Human Decision

## Findings from this wiki pass

No blocking doctrine conflicts were identified for this first-pass wiki build.

## Non-blocking follow-up candidates

- Decide whether to add a permanent link from root `README.md` to `docs/wiki/README.md` after the repository is adapted from skeleton to a concrete project.
- Decide how downstream projects should define project-specific full gates beyond `pmp_check` + `pytest`.
- Decide whether to classify release-note authority more formally once versioning/release automation expands.
